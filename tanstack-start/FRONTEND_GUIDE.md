# Frontend guide

This file is retained as a compatibility pointer only.

The current project rules live in:

- `AGENTS.md` — mandatory short index and task workflow;
- `.docs/architecture.md` — layers, dependency direction, and ownership;
- other `.docs/*.md` files — authoritative topic-specific patterns.

Do not add architectural rules here. Keeping a second full guide causes agents
and developers to follow stale, conflicting instructions.
